# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_KEY=your-azure-openai-key-here

# CosmosDB Configuration
COSMOS_DB_CONNECTION_STRING=AccountEndpoint=https://your-cosmosdb-account.documents.azure.com:443/;AccountKey=your-cosmosdb-key-here;

# Deployment Names
EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-small
CHAT_DEPLOYMENT_NAME=gpt-4o

# Optional: Model for enrichment (summarization, sentiment, keywords, image alt text)
# Uses CHAT_DEPLOYMENT_NAME if not specified
# LLM_MODEL=gpt-4o
